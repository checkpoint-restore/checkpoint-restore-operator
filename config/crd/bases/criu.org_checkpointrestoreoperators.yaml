---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: checkpointrestoreoperators.criu.org
spec:
  group: criu.org
  names:
    kind: CheckpointRestoreOperator
    listKind: CheckpointRestoreOperatorList
    plural: checkpointrestoreoperators
    singular: checkpointrestoreoperator
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CheckpointRestoreOperator is the Schema for the checkpointrestoreoperators
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CheckpointRestoreOperator.
            properties:
              applyPoliciesImmediately:
                description: |-
                  applyPoliciesImmediately controls whether garbage-collection policies
                  are applied as soon as they change, rather than only on the next
                  checkpoint event.
                type: boolean
              checkpointDirectory:
                description: |-
                  checkpointDirectory is the directory on each node where checkpoint
                  archives are stored.
                type: string
              containerPolicies:
                description: |-
                  containerPolicies defines retention limits for checkpoints of specific
                  containers.
                items:
                  properties:
                    container:
                      description: container is the name of the container this policy
                        applies to.
                      type: string
                    maxCheckpointSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        maxCheckpointSize is the maximum size of a single checkpoint archive for
                        the container.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    maxCheckpoints:
                      description: |-
                        maxCheckpoints is the maximum number of checkpoints to retain for the
                        container.
                      type: integer
                    maxTotalSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        maxTotalSize is the maximum combined size of checkpoints retained for the
                        container.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    namespace:
                      description: namespace is the namespace of the container this
                        policy applies to.
                      type: string
                    pod:
                      description: pod is the name of the pod this policy applies
                        to.
                      type: string
                    retainOrphan:
                      description: |-
                        retainOrphan controls whether checkpoints are kept after the container
                        no longer exists.
                      type: boolean
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              globalPolicy:
                description: |-
                  globalPolicy defines the retention limits applied to all checkpoints
                  that are not matched by a more specific policy.
                properties:
                  maxCheckpointSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: maxCheckpointSize is the maximum size of a single
                      checkpoint archive.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxCheckpointsPerContainer:
                    description: |-
                      maxCheckpointsPerContainer is the maximum number of checkpoints to
                      retain per container.
                    type: integer
                  maxCheckpointsPerNamespace:
                    description: |-
                      maxCheckpointsPerNamespace is the maximum number of checkpoints to
                      retain per namespace.
                    type: integer
                  maxCheckpointsPerPod:
                    description: |-
                      maxCheckpointsPerPod is the maximum number of checkpoints to retain per
                      pod.
                    type: integer
                  maxTotalSizePerContainer:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      maxTotalSizePerContainer is the maximum combined size of checkpoints
                      retained per container.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxTotalSizePerNamespace:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      maxTotalSizePerNamespace is the maximum combined size of checkpoints
                      retained per namespace.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxTotalSizePerPod:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      maxTotalSizePerPod is the maximum combined size of checkpoints retained
                      per pod.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  retainOrphan:
                    description: |-
                      retainOrphan controls whether checkpoints are kept after their source
                      container, pod or namespace no longer exists.
                    type: boolean
                type: object
              namespacePolicies:
                description: |-
                  namespacePolicies defines retention limits for checkpoints in specific
                  namespaces.
                items:
                  properties:
                    maxCheckpointSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        maxCheckpointSize is the maximum size of a single checkpoint archive in
                        the namespace.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    maxCheckpoints:
                      description: |-
                        maxCheckpoints is the maximum number of checkpoints to retain in the
                        namespace.
                      type: integer
                    maxTotalSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        maxTotalSize is the maximum combined size of checkpoints retained in the
                        namespace.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    namespace:
                      description: namespace is the namespace this policy applies
                        to.
                      type: string
                    retainOrphan:
                      description: |-
                        retainOrphan controls whether checkpoints are kept after their source no
                        longer exists.
                      type: boolean
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              podPolicies:
                description: podPolicies defines retention limits for checkpoints
                  of specific pods.
                items:
                  properties:
                    maxCheckpointSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        maxCheckpointSize is the maximum size of a single checkpoint archive for
                        the pod.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    maxCheckpoints:
                      description: |-
                        maxCheckpoints is the maximum number of checkpoints to retain for the
                        pod.
                      type: integer
                    maxTotalSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        maxTotalSize is the maximum combined size of checkpoints retained for the
                        pod.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    namespace:
                      description: namespace is the namespace of the pod this policy
                        applies to.
                      type: string
                    pod:
                      description: pod is the name of the pod this policy applies
                        to.
                      type: string
                    retainOrphan:
                      description: |-
                        retainOrphan controls whether checkpoints are kept after the pod no
                        longer exists.
                      type: boolean
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
          status:
            description: status defines the observed state of CheckpointRestoreOperator.
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
