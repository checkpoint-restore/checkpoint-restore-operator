apiVersion: criu.org/v1
kind: ForensicSnapshotChain
metadata:
  name: max-duration-test
spec:
  namespace: default
  selector:
    matchLabels:
      app: nginx
  capture:
    interval: 5s
    maxDuration: 15s