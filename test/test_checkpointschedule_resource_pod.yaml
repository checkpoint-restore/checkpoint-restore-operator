apiVersion: v1
kind: Pod
metadata:
  name: resource-test-pod
  namespace: default
  labels:
    app: resource-test
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.9
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
